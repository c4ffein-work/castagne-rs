# Minimal Test Fighter - No skeleton, just basics
# For testing raw engine functionality

:Character:
Name: Minimal Test Fighter
Author: E2E Tests
Description: Absolute minimum fighter for engine testing

:Variables:
var Health(Int): 1000
var StateTimer(Int): 0

:Init:
---Action:
Set(Health, 1000)
ChangeState(Idle)

:Idle:
---Action:
# Idle state

:LightPunch:
---Init:
Set(StateTimer, 5)
---Action:
Add(StateTimer, -1)
# When timer hits 0, should transition to Idle
IfLt(StateTimer, 1):
	ChangeState(Idle)
